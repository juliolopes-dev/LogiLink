/**
 * Rotas de DRP
 * 
 * Este módulo exporta as rotas de DRP:
 * - DRP por Produto (inclui /calcular, /grupos, /combinados)
 * 
 * Nota: DRP por NF está em /routes/nf-entrada.ts
 */

export { default as drpProdutoRoutes } from './produto.routes'
